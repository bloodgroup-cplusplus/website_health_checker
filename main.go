package main 
import (
	"github.com/urfave/cli/v2"
)


func main(){
	app := &cli.App{
		Name: "Healthchecker",
		Usage: "A tiny tool that checks whether a website is up and running or not "
		Flags:
		Action:
	}
}

